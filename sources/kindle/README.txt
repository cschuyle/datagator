Go to https://www.amazon.com/hz/mycd/digital-console/contentlist/booksAll/dateDsc/
For each page
    Copy the list
    Paste into kindle.txt
Done

`transform.sh` will do the work on the pipeline, so test it locally:

```
./transform.sh
```

See the output:

```
less ../../tmp/troves/kindle.json
```


